# 260413 19:00 세션

## 논의 주제
- MPL_H5_v7 학습 실패 원인 분석
- VIC_PHASE_04 (성공) vs im_walk_mpl.yaml (실패) config 비교
- 8GB VRAM 제약 하 config 최적화 시도 (v8, v9)
- H5 데이터 root orientation 고정 문제 발견

## 결정사항
- **학습 실패 근본 원인**: im_walk_mpl.yaml이 PHC 기본값 대비 극단적으로 축소된 config (MLP [512,512,256] vs [1024,1024,512,512], AMP buffer 2000 vs 200000)
- **8GB VRAM 한계**: VIC_PHASE_04 수준 config 재현 불가. 512 envs, 200k buffer, 4층 MLP 모두 OOM
- **v8 시도**: [1024,512] MLP + 10k buffer + 64 envs → 5000 epoch 학습 완료, ep_len ~11.9 (여전히 즉시 넘어짐)
- **v9 시도**: episode length 900→150 (5초), [512,256] MLP + 10k buffer + 128 envs → 학습 중
- **H5 root orientation 버그 발견**: h5_to_motion_lib.py 276번 줄에서 모든 프레임에 동일한 고정 upright rotation 적용. pelvis tilt/obliquity/rotation이 무시됨. 단, 즉시 넘어지는 현상과는 직접 관련 없을 수 있음

## H5 데이터 분석 결과
- Root orientation: 모든 프레임/클립에서 동일 (std=0.0°, mean=120.0°) — 비정상
- AMASS는 root orientation이 자연스럽게 변함 (std=30.5°)
- 보행 방향 (X축)과 body forward (rot*[0,0,1]) 정렬 확인 — 정상
- Vicon PiG pelvis 각도 데이터 존재: tilt ±10°, obliquity ±6°, rotation ±10°

## Config 변경 이력
| 버전 | MLP | AMP Buffer | num_envs | ep_length | 결과 |
|------|-----|------------|----------|-----------|------|
| v7 | [512,512,256] | 2,000 | 128 | 900 | ep_len ~10, 실패 |
| v8 | [1024,512] | 10,000 | 64 | 900 | ep_len ~11.9, 실패 |
| v9 | [512,256] | 10,000 | 128 | 150 | 학습 중 |

## 미해결 이슈
- 모든 버전에서 ep_len ~10-12 (즉시 넘어짐), 근본 원인 미해결
- 8GB VRAM으로 충분한 학습 가능한지 불확실
- h5_to_motion_lib.py root orientation 수정 필요 (pelvis 각도 반영)
- AMASS 데이터를 사용한 비교 테스트가 config 문제인지 데이터 문제인지 명확히 해줄 수 있음 (단, 사용자 허락 필요)

## 추가 진행 (v9 → v10)
- v9: 15k epoch 연장 시도, epoch 10235에서 OOM crash. ep_len ~10-11로 완전히 flat
- **h5_to_motion_lib.py root orientation 수정 완료** (276번 줄):
  - 이전: 모든 프레임에 동일한 upright rotation (std=0.0°)
  - 수정: Vicon pelvis tilt/obliquity/rotation을 per-frame으로 반영 (std=5.2°)
  - 수정 후 모션 라이브러리 재생성 (330 clips)
- v10 학습 시작: 수정된 H5 데이터, 128 envs, [512,256] MLP, 10k buffers, 15k epochs

## 다음 단계
- v10 학습 결과 모니터링 (ep_len 변화 여부가 핵심)
- root orientation 부호(sign) 확인 필요할 수 있음
- 효과 없으면 AMASS 비교 테스트 논의
