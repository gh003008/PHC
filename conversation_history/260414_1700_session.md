# 260414 17:00 세션

## 논의 주제
1. 서버 이전 실행 단계 (SSH key, git clone, data rsync)
2. 서버 Claude가 읽을 문서 체계 구성 (SERVER_GUIDE.md)
3. 서버 환경 규칙/Slurm 룰을 Claude가 자동으로 따르게 만들기

## 구현 완료 사항

### 서버 셋업 진행
- 서버에서 SSH key 생성 (`~/.ssh/id_ed25519`)
- GitHub 계정 `yjm7845123` (gh003008/PHC 저장소 collaborator)에 SSH key 등록
- `git clone git@github.com:gh003008/PHC.git` 완료, Jimin branch checkout
- rsync 완료: `data/` (3.5GB), `sample_data/` (303MB), `isaacgym/` (656MB), `phc_env_clean.yml`

### 서버용 문서 체계 구축
- **`PHC/SERVER_GUIDE.md` 신규 작성** (8개 섹션):
  1. Server Overview (spec, web monitor URL)
  2. Golden Rule: sbatch만 사용
  3. Home & Data Layout (jiminyoun@server1 디렉토리 구조)
  4. Environment Setup 절차 (conda + pytorch + isaacgym + phc editable)
  5. Running PHC Training (원본 config 사용 sbatch 스크립트 템플릿)
  6. Git Workflow (code=git, data=rsync)
  7. Migration Status 체크리스트
  8. Troubleshooting
- **`PHC/CLAUDE.md` 상단에 Environment Detection 섹션 추가**:
  - `hostname`이 `server1`이면 SERVER_GUIDE.md 읽기 지시
  - 로컬이면 shrunk config 사용 유지
  - `conversation_history/INDEX.md`를 세션 시작 시 읽기 명시

## 결정사항

### 서버 Claude가 알아야 할 3종 정보 체계
| 정보 | 저장 위치 | 전달 경로 |
|---|---|---|
| 서버 환경 규칙 (Slurm, 리소스 제한) | `PHC/SERVER_GUIDE.md` | git pull |
| PHC 프로젝트 지시사항 | `PHC/CLAUDE.md` | git pull |
| 진행 중 대화 맥락 | `PHC/conversation_history/` | git pull |
| 워크스페이스 대화기록 규칙 | (로컬 전용 `CLAUDE.md`) | 필요시 서버에 별도 복사 |

### 서버 첫 학습 sbatch 템플릿 확정
- 자원: `--gres=gpu:1 --cpus-per-task=8 --mem=32G -t 12:00:00`
- 실행: `python phc/run_hydra.py learning=im_walk env=env_im_walk exp_name=PHC_Server_Baseline_v1 num_envs=512 headless=True`
- **원본 config** (shrunk 아님) 사용이 핵심: MLP [1024,1024,512,512], num_envs 512, AMP buffer 200k, episode_length 300

### 가설 검증 설계
원본 config로 AMASS 학습 성공 시 → "config 축소가 학습 실패의 원인" 확정.
실패 시 → 더 근본적 원인 조사 (stateInit, VIC 관련 params 등).

## 미해결 이슈

### 서버 측 즉시
- [ ] 서버 conda env 구축 (`conda env create -n phc -f ~/phc_env_clean.yml`)
- [ ] PyTorch + CUDA 12.1 재설치
- [ ] Isaac Gym editable install (`~/isaacgym/python && pip install -e .`)
- [ ] PHC editable install (`~/PHC && pip install -e .`)
- [ ] GPU 검증 (`torch.cuda.is_available()`, `device_count() == 4`)

### 서버 측 학습
- [ ] `~/PHC/train_phc.sh` sbatch 파일 작성 (SERVER_GUIDE.md §5 템플릿 기반)
- [ ] `mkdir -p ~/PHC/logs`
- [ ] `sbatch train_phc.sh` 제출
- [ ] `squeue -u $USER`로 RUNNING 확인
- [ ] 1000 epoch 경과 후 ep_len 측정 → "원본 config 성공" 가설 검증

### 로컬 문서 동기화
- [ ] 이 세션(260414_1700) 파일을 GitHub push (서버에서 읽기 위함)
- [ ] conversation_history/INDEX.md 업데이트

## 다음 단계
1. 이 세션 파일 commit + push
2. 서버에서 `cd ~/PHC && git pull origin Jimin`
3. 서버에서 `cat SERVER_GUIDE.md` 읽고 그대로 따라 conda env 구축
4. 첫 sbatch 제출 후 squeue/log 모니터링

## 세션 종료 시 상태
- 로컬: SERVER_GUIDE.md 작성 완료, CLAUDE.md 업데이트 완료, commit 대기
- 서버: 코드+데이터 완료, env 구축 대기
