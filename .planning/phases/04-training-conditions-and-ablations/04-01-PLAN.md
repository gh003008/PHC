# Plan 04-01: Training Conditions And Ablations

**Phase:** 4 - Training Conditions And Ablations
**Status:** Accepted

## Goal

Create a reproducible v1.0 training/ablation command matrix and ensure the code
supports the required conditions.

## Tasks

1. Ensure reward computation still works when pain observation is disabled.
2. Document the main `pain_obs_on + pain_reward_on` condition.
3. Document `pain_obs_off + pain_reward_on`.
4. Document `pain_obs_on + pain_reward_off`.
5. Document left/right side-specific impairment runs.
6. State that guard-enabled runs are debug-only.

## Verification

- `python3 -m py_compile phc/env/tasks/humanoid_im_pain.py`
- Confirm command matrix includes all required condition labels and Hydra
  overrides.
- `git diff --check`

## Non-Goals

- Do not run full PPO training.
- Do not generate evaluation plots.
