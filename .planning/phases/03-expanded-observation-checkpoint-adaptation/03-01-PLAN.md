# Plan 03-01: Expanded-Observation Checkpoint Adaptation

**Phase:** 3 - Expanded-Observation Checkpoint Adaptation
**Status:** Accepted

## Goal

Make PHC-Pain-v1 checkpoint restore copy pretrained PHC observation weights and
zero-initialize new pain-observation columns.

## Tasks

1. Detect v1 expanded observation tasks through `get_pain_obs_size()`.
2. Adapt model state dict tensors with wider input dimensions.
3. Adapt input running mean/std for expanded observation dimensions.
4. Reset optimizer state when adaptation occurs.
5. Add v1 checkpoint adaptation config documentation.

## Verification

- `python3 -m py_compile phc/learning/amp_agent.py phc/env/tasks/humanoid_im_pain.py`
- Config sanity check for checkpoint adaptation settings.
- `git diff --check`

## Non-Goals

- Do not run a full checkpoint restore under IsaacGym in this phase.
- Do not change network builder architecture.
